import { Page } from "page.slint";
export component MapPage inherits Page {
    has-back-button: true;
    header: @tr("返回首页");

    in property <int> map_width;
    in property <int> map_hight;

    // for a in rang(): Rectangle {
        
    // }
    GridLayout {

    }
}
